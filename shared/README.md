# Shared

This folder will hold shared schemas and contracts when the frontend and
backend need the same shape for lessons, runtime events, or validation results.

## Current Contracts

- `schemas/lesson.schema.json`: V1 quest metadata, starter code, expected
  output, required concepts, learning goals, hints, and adult guidance.

Runtime events are currently documented in `docs/runtime-events.md`.
